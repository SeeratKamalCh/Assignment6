
This code processes an image of a rubiks cube and attempts to apply caliberation
technique to find the projection matrix P and instrinsic K and extrinsic 
matrices R of the camera

When the code is run it runs the caliberation method and displays the results.